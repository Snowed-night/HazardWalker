# 待确认与待裁决清单（2026-07-23）

本清单只记录当前无法靠仓库静态检查确认的事项。未列出的文档不因此自动变为已验证；当前唯一已确认的平台操作入口仍是 [官方 SimEnv 平台环境使用手册](../官方SimEnv平台环境使用手册.md)。

## 一、环境与账号资料（需管理员复验）

| 项目 | 当前状态 | 需要的证据 | 处理建议 |
|---|---|---|---|
| 主力机 SSH | 仅有历史记录 | 当前主机名/地址、账号权限、密钥策略、可达性 | 复验后合并为一份正式 SSH 手册 |
| 远程桌面 | 仅有历史记录，凭据已删除 | 当前 RDP 网关、图形环境、账号权限和断开恢复方式 | 复验后合并为一份正式远程桌面手册 |
| 主力机环境 | 仅有 7 月历史快照 | ROS、容器、GPU、共享目录、服务健康状态 | 由平台/集成组出具一次可复验验收记录 |

## 二、平台与官方仿真（需在线实测）

各组已有证据与缺口已统一到 [官方 SimEnv 验收状态与证据缺口](../官方SimEnv验收状态与证据缺口.md)，本节只保留待裁决项。

| 项目 | 静态检查结论 | 尚缺证据 |
|---|---|---|
| 当前官方接入链路 | 文档、脚本和平台包均已改为 `rosbridge_websocket` + ROS2 `/hw/*`，不应再依赖容器内 `ros1_bridge dynamic_bridge` | 在独占官方会话中逐段验证 RGB、深度、内参、雷达、里程计、控制与进程清理 |
| 控制与导航闭环 | 感知侧不占用 `/hw/cmd_vel` 的代码契约已通过离线测试 | 导航组仍需以合法 SLAM 位姿完成真实楼宇探索、复查动作和返航证据 |
| 三维定位 | 离线深度反投影、坐标变换与真值隔离契约已通过 | 在官方随机场景以自建合法 SLAM 位姿完成端到端定位误差记录 |

## 三、代码与分支（需负责人决定）

| 项目 | 发现 | 建议 |
|---|---|---|
| 当前工作分支与 `origin/dev` | `agent/perception-only-followup` 相对 `origin/dev` 落后 1 个提交、领先 5 个提交；工作区尚有大量未提交的感知结果整理 | 先按功能拆分并审阅未提交内容，再决定合并 `origin/dev` 或 rebase；不要直接清理工作区 |
| 桌面 `HazardWalker` 工作树 | 仍在 `dev`，且保留独立的 `docs/README.md` 修改、`AGENTS.md` 和旧路径下的平台手册 | 当前不覆盖；先决定是否将桌面树仅作为 Git 元数据/备份，再在一次明确同步中处理 |
| 旧适配实现 | 旧 ROS1 adapter、JSON pipe、topic relay 与 dynamic_bridge 已移除；仅保留 `run_official_simenv_rosbridge_adapter.sh` 与 `official_simenv_rosbridge_ros2_adapter_node.py` | 仍需在最新代码的独占官方会话中实测当前唯一适配链 |
| 远程平台分支 | 2026-07-23 获取到的 `origin/feature/platform` 仅相对 `origin/dev` 改动 1 个 Python 文件 | 在 PR 中说明该文件用途、测试与风险；未审核前不应作为当前平台基线 |

## 四、文档待处理项

- `docs/groups/` 是阶段提交与技术记录。各组应在下一次提交前，将“已完成/已解决”改为可追溯的测试结论，并附 Git 版本、日期、输入、输出和失败边界。
- `docs/history/` 中的 Harmonic、旧双栈和旧主力机资料仅为开发路径记录；若未来确需恢复，必须先按当前官方手册和实际代码逐项复验。
- 官方包内 `ros2_ws/src/hazardwalker_platform/docs/` 是赛题/平台参考资料，保留原位，不与团队 `guidebook/` 重复维护。
