# 决策组文档

负责范围：

- 任务 FSM。
- 探索、检测、确认、重观察、返航、超时和失败状态。
- 结果收集与输出触发。
- 与导航、感知、测试组的状态接口。

当前边界：

1. 候选目标、已确认危险源、重观察、探索完成和返航必须是不同状态，候选不得直接输出为结果。
2. 任务输出必须附合法位姿来源、坐标系、时间戳和感知确认依据；缺失时应失败关闭，不伪造结果。
3. 当前最小状态收集仅作离线回归，正式闭环须与导航、感知和平台的真实运行记录一同验收。
