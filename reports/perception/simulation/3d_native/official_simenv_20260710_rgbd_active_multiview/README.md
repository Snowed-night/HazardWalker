# official_simenv_20260710_rgbd_active_multiview

> **历史内部回归，禁止作为官方成绩。** 该套素材保留用于观察主动复查流程；旧视角标签未能
> 证明合格的水平基线与 25° 侧向视差。后续重跑放入 `reruns/YYYYMMDD_<seed>/`，不得覆盖本基线。

本目录只记录官方 SimEnv 原生复杂房间实验；保留墙体、门、家具、立柱、纵深和自然遮挡。

- 用例：20（旧版记录）
- **2026-07-12 严格审计：本目录的旧 20/20 不能作为有效成绩。** 旧视角标签将微小竖直起伏误算为独立视角；已在算法中修正为仅统计水平基线与朝向，待稳定平台后必须同协议重跑。
- 不读取 `danger_truth.json` 或私有布局真值。
- 多视角用例只有在 Gazebo 机器人世界位姿实际变化后才计为有效视角。
- 黄色 `reobserve` 是待复查候选，不等于确认识别。
