# official_simenv_20260710_multi_ball_clutter

> **历史内部回归，禁止作为官方成绩。** 本套 10 场共记录 47 个目标的定位，但仅 7 个完成旧协议确认；
> 它用于展示粘连分割、漏分与复查恢复的优化路径。后续重跑放入 `reruns/YYYYMMDD_<seed>/`。

本目录只记录官方 SimEnv 原生复杂房间实验；保留墙体、门、家具、立柱、纵深和自然遮挡。

- 用例：10
- 通过：10
- 待复核：0
- 不读取 `danger_truth.json` 或私有布局真值。
- 多视角用例只有在 Gazebo 机器人世界位姿实际变化后才计为有效视角。
- 黄色 `reobserve` 是待复查候选，不等于确认识别。
