# 官方 SimEnv Gazebo Classic：red_ball_3d_localization

> 证据类别：内部回归；人工生成受控物体，不属于官方随机场景全流程成绩。
> A阶段固定交付标签为20260725，真实运行时间见下方；正例均为半径0.15 m红球。

本目录的每张原图和标注图都由当前官方 ROS1/Gazebo Classic 世界的 `/hw/*` 话题采集。模型只在隔离容器中临时生成；案例结束后删除。

- 案例数：5
- 通过/失败：5/0
- ROS_DOMAIN_ID：78
- 真实运行：2026-07-20T04:13:31.747506+00:00 至 2026-07-20T04:14:20.896260+00:00（49.149 s）
- SEED：2026071802
- Git：b69613ec8543+worktree-752b61be
- 定位评估帧：base
- 临时夹具中心：[0.0702018487922652, 1.1105965025442044, 0.15]（map->real_sense camera_forward 3.000m (test fixture only)）
- 失败案例保留在 `cases.csv` 和 `images/`，不得删除或改写为成功。

## 结论与边界

- 5/5场景得到严格红球候选和唯一base坐标，候选召回率100%，无重复轨迹。
- 平均定位误差0.0402 m，最大0.0644 m，低于官方1 m要求。
- 运行期只使用RGB、深度、相机内参和公开静态 `base<-real_sense` 外参；
  受控SDF真值只在快照落盘后用于评估，未传入检测器。
- 当前没有导航组合法SLAM动态位姿，因此只证明base局部定位，未宣称map/world定位，
  也没有写 `results/detected_danger.json`。
- `images/` 同时保留每例原图、标注图、毫米深度图和深度可视化；
  `snapshots/`、`logs/` 保存逐例消息与解析参数。
