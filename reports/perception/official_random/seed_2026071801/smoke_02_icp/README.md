# 官方随机场景 SEED=2026071801 感知烟测 02（ICP）

状态：定位改进验证通过，场景任务仍未完成；不计入官方全流程成绩。

## 运行范围

- 使用与 `smoke_01` 相同的官方三层随机场景种子重新启动。
- 运行期只使用公开 RGB-D、相机内参、`/scan`、`/trunk_imu` 和自建 TF。
- 平移定位由“累计占据图相关”升级为“IMU锁定旋转 + 相邻帧鲁棒 ICP”，
  历史占据图只在 ICP 证据不足时回退。
- 执行两段 IMU 闭环自主环视，不读取布局、manifest、危险源真值或 Gazebo 位姿。

## 真实结果

- 离线回归：`184 passed, 0 failed`。
- 第一段目标 `65°`，实际 `65.045°`，耗时 `16.99 s`，成功结束。
- 第二段目标 `65°`，实际 `26.81°`，55 秒超时；现场截图显示 A1 位于入口墙角，
  不继续强转，脚本已自动停车。
- 两段累计约 `91.855°` 的真实视角变化中没有红色候选或确认目标。
- `selected_images/` 和 `selected_depth/` 保存 8 组真实复杂环境 RGB-D 上下文证据。

## 定位改进

独立评估器仅在诊断采样时读取 Gazebo 位姿，数据从未回灌算法：

- 环视前平面误差：`0.0374 m`。
- 第一段 `65.045°` 后平面误差：`0.1181 m`。
- 两段累计后平面误差：`0.1350 m`。
- 同种子旧算法 `smoke_01` 在约 `65.5°` 后误差约 `0.7280 m`。

这证明相邻帧 ICP 显著降低了入口旋转时的二维漂移，但没有证明多楼层 z 定位、
完整探索、红球召回率或正式得分。

## 代码版本

- Git 基线：`a62fe7e`。
- 运行覆盖包：
  `SHA256 9e8743bb28abc9f0b40acb9b659ae197ac6dbc5433419187d989401b0e4fd6d5`。
- 结构化定位诊断见 `postrun_localization_diagnostic.json`。

当前版本独立验收结果见 `independent_post_evaluation.json`。该轮因缺少任务完成契约、
未收到 `FINISHED`、无候选且无确认红球而被明确判为不合格，不能作为完整任务成绩。
