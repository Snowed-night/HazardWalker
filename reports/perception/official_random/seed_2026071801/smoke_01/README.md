# 官方随机场景 SEED=2026071801 感知烟测 01

状态：失败样本，仅用于算法优化路径，不计入官方全流程成绩。

## 运行范围

- 场景由官方 `auto_headless.sh` 以 `SEED=2026071801` 生成，三层、每层四个房间。
- 运行期感知只订阅公开 RGB、深度、相机内参、`/scan`、`/trunk_imu` 和自建
  `start -> real_sense` TF。
- 没有读取场景布局、manifest、危险源真值或 Gazebo 真值位姿。
- 入口处执行了公开 IMU 闭环的自主原地环视；不是完整楼宇探索。

## 真实结果

- 感知记录 `1890` 帧，合法定位轨迹 `178` 个样本。
- 入口视野及 `65.518°` 实际环视范围内没有红色候选，也没有确认目标。
- 360° 环视因 45 秒超时终止；官方 A1 对 `0.45 rad/s` 指令响应明显偏慢。
- `detected_danger.json` 为空；这只表示本次入口烟测没有确认目标，不能推导整栋楼召回率。
- `selected_images/` 保存 12 张真实复杂环境上下文标注图，
  `selected_depth/` 保存对应原始深度 `.npy`。

## 代码版本

- Git 基线：`a62fe7e`。
- 感知/记录器运行覆盖包：`SHA256 6968f71b79d363780c0dc11b9d90402a684d40482c8c797b6f14dcd5bf123158`。
- 自主环视脚本覆盖包：`SHA256 8626706859203ea80463796ad5ccb91fbbd12dce5eb0e1c80d29eb4bfd99f8fe`。

## 赛后诊断

运行结束后才读取裁判位姿做独立定位误差诊断；该数据从未回灌感知、定位或控制算法。
结果见 `postrun_localization_diagnostic.json`：二维相对位姿误差约 `0.728 m`，
已接近官方 `1.0 m` 匹配阈值，因此下一轮改为 IMU 锁定旋转的相邻帧 ICP。

当前版本独立验收结果见 `independent_post_evaluation.json`。该轮因缺少任务完成契约、
未收到 `FINISHED`、无候选且无确认红球而被明确判为不合格，不能作为完整任务成绩。
