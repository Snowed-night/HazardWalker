# 官方 SimEnv ROS1/ROS2 适配设计与待验收记录

负责人：姜晨。记录日期：2026-07-14。

## 本次内容

新增官方 ROS1 容器内的原生消息中继、安全 `/hw/cmd_vel` 门、ROS1/ROS2 分段验收脚本、离线映射契约和三项
候选平台补丁。适配节点保留图像和点云完整 ROS 消息，不使用旧 JSON 分片管道。

## 本次未做出的结论

本记录不是运行通过证据。尚未在官方 A1 控制器上完成 ROS1 直连前进 ≥1m、转向、停止；尚未证明 RGB、深度、
内参、里程计进入 ROS2 `/hw/*`；尚未证明 ROS2 控制或完整业务闭环。此前 headless 日志存在渲染关闭导致深度
相机不发布的风险，且 `/cmd_vel` 曾只观察到桥接订阅者。

## 后续验收素材

平台组在独立官方环境执行 `scripts/verify_official_simenv_ros1_adapter.sh` 后，须在同一目录创建新的带时间戳
子目录，保存 `summary.json`、测试 CSV、控制器/里程计文本日志和截图或视频索引。原始 rosbag、视频放共享盘，
README 写明路径与校验信息。
