# 官方 SimEnv headless 控制入口修复复测

负责人：姜晨。该目录记录 2026-07-14 对官方 ROS1 容器的实际复测，不替代 ROS2 适配或完整任务验收。

- `summary.json`：传感器频率、控制器补丁、同轮里程计和结论。
- `testing_record_platform.csv`：供测试组汇总的状态表。

结论为 `partial`：完整官方工作空间、Xvfb 前置启动和物理取消暂停后，传感器已恢复；将 headless
控制器显式置入 RL 状态后，非零 `/cmd_vel` 已产生可测位移和转向。当前场景位置不适合判断变速能力，
且没有达到 1 m 直行/0.2 rad 转向阈值或视频证据，不能宣称直连控制、ROS2 控制或闭环任务已通过。
