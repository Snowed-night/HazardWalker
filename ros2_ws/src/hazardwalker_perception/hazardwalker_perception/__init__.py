
"""hazardwalker_perception 包初始化文件。

所属组：感知组。
文件作用：
- 标记 `hazardwalker_perception` 是一个 Python 包。
- 感知算法函数和 ROS 节点分别放在独立模块中。

当前模块：
- `red_ball_detector.py`：红球检测离线函数。
- `hsv_detector_node.py`：HSV 检测 ROS 节点。

后续扩展：
- 可新增 `localize_hazard.py`、`track_hazards.py`、`debug_image.py`。
"""
