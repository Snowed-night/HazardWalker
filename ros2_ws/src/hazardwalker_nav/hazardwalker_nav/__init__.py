
"""hazardwalker_nav 包初始化文件。

所属组：导航组。
文件作用：
- 标记 `hazardwalker_nav` 是一个 Python 包。
- 导航纯函数和 ROS 节点分别放在独立模块中。

当前模块：
- `waypoint_controller.py`：固定航点控制纯函数。
- `waypoint_patrol_node.py`：固定航点 ROS 节点。

后续扩展：
- 可新增 `frontier_explorer.py`、`nav2_goal_adapter.py`、`stuck_recovery.py`。
"""
