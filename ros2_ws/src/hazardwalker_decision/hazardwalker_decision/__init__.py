
"""hazardwalker_decision 包初始化文件。

所属组：决策组。
文件作用：
- 标记 `hazardwalker_decision` 是一个 Python 包。
- 决策纯函数和 ROS 节点分别放在独立模块中。

当前模块：
- `result_builder.py`：任务结果结构构建函数。
- `mission_state_machine_node.py`：任务状态机和结果写入 ROS 节点。

后续扩展：
- 可新增 `state_machine.py`、`target_selection.py`、`belief_map.py`。
"""
