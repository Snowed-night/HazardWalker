# ROS 2 Workspace

ROS 2 工作空间目录。

## 目录说明

- `src/`：ROS 2 功能包源码。
- `build/`：编译产物。
- `install/`：安装产物。
- `log/`：构建日志。

推荐源码放在：

```text
ros2_ws/src/
```

构建产物 `build/`、`install/`、`log/` 已在 `.gitignore` 中忽略，不要提交。
