# HazardWalker Agent Instructions

本文档是 HazardWalker 仓库级 AI 助手规则。进入仓库后先阅读本文件，再结合根目录`README.md`、`docs/README.md`、当前 Git 状态和用户最新要求开展工作。本文只保留当前有效的项目边界、目录规则、成果规则和提交规范；历史进度以仓库报告为准，不在本文件维护易过期状态。

## 1. 项目目标与边界

- 项目目标是在未知多层楼栋仿真环境中控制四足机器人自主探索，识别红色球体危险源，完成三维定位、去重、结果输出和返航。
- 比赛检测目标只有红色球体。官方干扰源只有红色立方体和绿色球体。红色圆柱、圆锥、方块、平板和不规则物体只用于虚警压力测试，不能作为危险源输出。
- 官方环境为 ROS1 Noetic、Gazebo Classic 和 Unitree A1；HazardWalker 业务层为 ROS2 Jazzy。
- 平台适配统一由 `hazardwalker_platform` 提供。导航、感知和决策只面向稳定的 `/hw/*` 接口，不直接绑定官方容器内部实现。
- 只能使用官方允许的 RGB、深度、相机内参、激光、IMU、团队 SLAM 位姿和公开接口。严禁在运行期读取危险源真值、Gazebo 真值里程计、内部场景文件或硬编码目标位置。
- 平台可用、SLAM 可用、导航可用、感知可用和整场任务闭环是不同结论，不得相互替代或夸大。

## 2. 本地目录与 Worktree 规范

当前 Git 只登记两个目录：

```text
C:\hwpdepth
C:\Users\jiangchen\OneDrive\Desktop\HazardWalker
```

具体规则：

1. `C:\hwpdepth` 是唯一主开发目录。所有后续代码修改、测试、仿真和成果整理统一在这里进行。
2. 桌面 `HazardWalker` 暂时只保存公共 `.git` 元数据和同步副本，不用于开发。
3. 每次 fetch、pull、merge、rebase、切换基线或更新本地仓库后，两个目录都必须同步到对应的最新状态，并检查必要的本地文档和成果是否一致。
4. 临时 worktree 同时最多一个，固定使用 `C:\HazardWalker-scratch`。不得改用其他随机目录。
5. 临时任务结束后必须执行 `git worktree remove C:\HazardWalker-scratch`，再执行`git worktree prune` 并复核 `git worktree list`。
6. 禁止再创建用途不明的 `C:\hw*` 目录。
7. 在 `C:\hwpdepth` 转为具有独立 `.git` 目录的仓库前，不能删除桌面 `HazardWalker`，否则会破坏当前共享 Git 元数据和 worktree。

开始工作和结束同步时至少检查：

```powershell
git -C C:\hwpdepth status -sb
git -C C:\Users\jiangchen\OneDrive\Desktop\HazardWalker status -sb
git -C C:\Users\jiangchen\OneDrive\Desktop\HazardWalker worktree list
```

不要覆盖用户未提交改动。发现已有修改时，先判断来源、用途和是否已在远程保存；无法确认价值的内容交给用户判断。禁止使用 `git reset --hard` 或强制覆盖来处理未知改动。

## 3. 工作方式

1. 修改前阅读相关模块 README、配置、测试和最近实验报告，确认任务归属与已有结论。
2. 优先通过代码、日志、结构化记录和真实图片核实问题，不根据目录名或节点存活推断成功。
3. 诊断请求只定位并说明原因；用户明确要求修复或开发时，才修改代码并验证。
4. 对当前任务作必要的合理假设可以继续推进，但会改变技术路线、删除有价值成果或扩大外部影响时，必须先说明。
5. 修改公共结构、运行方式、接口或文档入口时，同步更新对应 README 和 `docs/README.md`。
6. 用户要求简洁回答时优先给结论；涉及风险、失败和未完成项时必须明确边界。

## 4. 感知成果保存规范

### 4.1 阶段与方向

- 成果按阶段保存，每个阶段再按多球粘连、部分遮挡、红色干扰物、多视角确认、三维定位等方向分开建立目录。
- 每个阶段只保留最终较好、能够说明结论的实验结果，不保存开发过程中随手生成的临时效果、调试截图和重复中间文件。
- 不同阶段的成果可以同时保留，用于展示算法开发路径。例如 7 月 5 日、7 月 10 日和 7 月 15 日属于不同阶段。
- 同一阶段、同一方向跑出更好结果后，可以用新结果替换该阶段较早且较差的结果；替换时必须同步更新 README、指标和测试记录，不能混用不同代码版本的数据。
- 用于正式结论的失败案例可以保留，但必须具有明确诊断价值、失败说明和后续修复关系；没有说明的临时失败输出应清理。

### 4.2 目录

所有感知仿真成果保存到 `reports/perception/`，不得只保留在 `work/`、系统临时目录或远程主机。每类实验按日期建立独立目录，例如：

```text
reports/perception/simulation/3d_native/official_simenv_YYYYMMDD_<实验类别>/
```

官方随机场景结果单独放在：

```text
reports/perception/official_random/
```

官方随机场景不得与内部专项实验混淆。已完成并形成结论的随机场景历史运行目录用于展示优化过程，新实验不得覆盖旧运行；临时探测、重复帧和未整理的原始文件不属于历史成果，应在阶段收尾时清理。

### 4.3 单个实验的最低交付

每个实验目录至少包含：

- 有代表性的原始图和检测标注图；
- `README.md`：实验环境、目的、方法、代码版本、结论和限制；
- `summary.json`：检出率、虚警、定位误差、耗时、帧率或当前实验适用的核心指标；
- 测试组规定格式的 `testing_record_perception.csv` 和对应 JSON 记录；
- 必要的输出结果、启动参数、固定 SEED、Git 版本及失败说明。

文件名必须明确场景、目标、视角、遮挡或检测结果，不能使用无法追溯的 `test1.png`、`latest.png` 等名称。报告中的图、表和 JSON 必须来自同一次运行，不能拼接不同参数或不同提交的结果冒充一轮实验。

大型 rosbag、原始 `.ppm`、未筛选帧序列、大视频和模型权重不提交 Git。确需保留时放入约定的大文件目录，并在实验 README 中记录路径、生成时间、SEED 和校验信息。

## 5. 感知代码设计规范

- 保持模块化设计，检测、跨帧确认、主动复查、三维定位和去重分别实现。
- 检测算法继续遵循 `DetectionBackend` 接口，便于替换或融合 HSV/OpenCV、传统几何、YOLO、分割模型或其他方案。
- 参数统一放入配置文件或 ROS 参数，不在代码中硬编码话题、阈值、路径、SEED 和设备地址。
- 新增或大幅修改的代码文件应添加简洁中文文件说明；关键算法、数据流、异常处理和可调整位置需写清楚中文注释。
- 输出结构、坐标系、时间戳和消息类型保持统一。候选目标只能输出为候选或重观察状态，不得直接伪装成已确认危险源。
- 做好空帧、无深度、RGB-D 不同步、TF 缺失、话题中断、坐标无效和重复目标等异常处理，禁止
  静默补值或伪造结果。
- 三维位置必须携带合法坐标系、时间戳和定位来源；缺少有效 TF 或深度时应明确失败或延迟确认。
- 主动复查只产生规范的重观察请求；实际运动由导航层结合避障和 SLAM 执行，感知节点不得绕过控制仲裁直接抢占 `/hw/cmd_vel`。

## 6. 测试与验证

- 每次修改同步补充或更新自动化测试，核心算法优先写成不依赖 ROS 的纯函数。
- 先运行离线回归，再在官方 SimEnv 公开输入下验证。没有官方环境时可以完成离线验证，但必须
  明确说明未进行实景验收。
- 常用离线入口：

  ```powershell
  python scripts/run_offline_tests.py
  ```

- 官方仿真必须记录本轮真实输入、启动参数、SEED、Git 提交、截图、结构化指标和失败原因。
- 节点存在、话题有发布者或地图能够保存，只能证明对应链路存在，不能单独证明识别、定位、建图、
  探索或任务闭环成功。
- 只有本轮自动探索、红球确认、三维定位、去重、结果输出和返航全部满足验收条件时，才能写成完整
  任务闭环通过。

## 7. 代码提交与 PR 规范

- 提交前检查工作区、远程差异、敏感信息、README 联动、自动化测试和成果目录。
- 不提交密码、Token、账号、模型大文件、rosbag、临时缓存、构建产物和无关生成文件。
- 不把 `work/` 中的临时脚本、预览图和试验副本直接提交；需要保留的能力应整理到正式脚本、
  测试或报告目录。
- 提交信息和 PR 使用简洁中文，说明修改目的、测试结果、官方仿真结论及成果目录。
- 不在 `dev` 上直接堆叠开发提交。功能开发使用明确命名的分支，经测试后向 `dev` 提交 PR。
- 只有用户明确要求时才提交、推送、创建或更新 PR。不得强推、重写公共历史或擅自合并。
- 合并或同步后复核 `C:\hwpdepth` 与桌面 `HazardWalker`，确保两个目录对应同一公共基线，必要的
  本地规范、手册和成果也已同步。

## 8. 安全与环境约束

- 账号、密码、SSH 私钥、Token、临时凭据和个人信息不得写入代码、报告、日志或 PR。
- 遇到登录、MFA、账号授权、付款或需要用户身份确认的步骤时暂停，让用户完成。
- 普通小型开发依赖可按任务需要安装；大型 SDK、模型权重、虚拟机和系统级组件先给出官方命令或
  下载地址，由用户确认。
- 不擅自重启、删除或抢占共享官方 SimEnv 容器。控制实验必须确认独占、控制器就绪和安全停止
  流程。
- 中文和 Unicode 文件按 UTF-8 处理；修改文件优先使用结构化工具和补丁，避免因 PowerShell
  管道或错误编码损坏内容。
